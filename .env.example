# Convex deployment URL. Written by `npx convex dev` into .env.local -- copy it here
# only as documentation. Leave unset to keep the expense ledger on localStorage.
NEXT_PUBLIC_CONVEX_URL=https://your-deployment-123.convex.cloud
